package com;

public class Handler {
    private int checkModeType(int mode) {
        if (mode == 1) {
            return mode;
        } else if (mode == 2) {
            return mode;
        } else {
            return 0;
        }
    }

}
